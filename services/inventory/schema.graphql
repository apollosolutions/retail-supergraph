extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key"])

type Variant @key(fields: "id") {
  id: String
  inventory: Inventory
}

type Inventory {
  inStock: Boolean
  inventory: Int
}
