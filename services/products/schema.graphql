extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key"])

type Query {
  search(searchInput: SearchInput): [Variant]
  variant(id: String): Variant
  product(id: String): Product
}

input SearchInput {
  titleStartsWith: String
  sizeStartsWith: String
}

type Product @key(fields: "id") {
  id: ID!
  variants: [Variant]
  title: String
  description: String
  mediaUrl: String
}

type Variant @key(fields: "id") {
  id: String
  product: Product
  colorway: String
  price: Float
  size: String
  dimensions: String
}
